{
  "name": "your-project-name",
  "version": "1.0.0",
  "main": "dist/server.js",  // Corrigido para apontar para o arquivo transpilado
  "scripts": {
    "dev": "ts-node-dev src/index.ts",  // Alterado para o arquivo correto
    "build": "tsc",
    "start": "node dist/index.js",  // Corrigido para apontar para o arquivo transpilado
    "prisma:generate": "prisma generate",
    "prisma:migrate": "prisma migrate dev --name init"
  },
  "dependencies": {
    "express": "^4.17.1",
    "@prisma/client": "^2.26.0",
    "dotenv": "^8.2.0"
  },
  "devDependencies": {
    "typescript": "^4.9.5",
    "ts-node": "^10.9.1",
    "ts-node-dev": "^2.0.0",
    "@types/node": "^18.0.0",
    "@types/express": "^4.17.14"
  }
}
